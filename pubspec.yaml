name: movies_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk:  ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: 
  google_fonts: 
  flutter_screenutil: 
  auto_route: 
  auto_route_generator: 

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner:
  flutter_lints: 

flutter:

  uses-material-design: true
  assets:
    - assets/images/

